extends layout.jade

block title
	title= title

block body
	h1 Edit Todo
	form(name="newTask", action="/Todo/Edit", method="post")
		.form-group
			label(for="Message") Title of todo:
			input(class="hidden" name="todo_id", value=todo[0].todo_id)
			input(class="hidden" name="status", value=0)
			input(class="form-control" name="message", value=todo[0].message, required)
		button(type="submit", class="btn btn-primary") Submit the change