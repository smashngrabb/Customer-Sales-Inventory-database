extends layout.jade

block title
	title= title

block body
	h1 Add new Todo
	form(name="newTask", action="/Todo/New", method="post")
		.form-group
			label(for="Message") Todo title:
			input(class="form-control" name="message", required)
		button(type="submit", class="btn btn-primary") Add new Todo